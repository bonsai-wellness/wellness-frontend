import { PuntoImportante } from './puntoImportante';

describe('PuntoImportante', () => {
  it('should create an instance for Punto Importante', () => {
    expect(new PuntoImportante()).toBeTruthy();
  });
});
